interface:
  display_name: "TrayPage"
  short_description: "Create, publish, and revise TrayPage pages"
  default_prompt: "Use $traypage to create this as a TrayPage page, prepare it for review, and publish it when ready."

policy:
  allow_implicit_invocation: true
